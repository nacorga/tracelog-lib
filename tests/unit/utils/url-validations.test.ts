import { describe, test } from 'vitest';

// v0.6.0: url-validations.utils removed - tests deprecated
describe.skip('URL Validations - DEPRECATED (utils removed in v0.6.0)', () => {
  test('deprecated', () => {
    // URL validation utilities no longer exist
  });
});
